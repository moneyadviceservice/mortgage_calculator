<table class="mortgagecalc__table stamp-duty__table">
  <thead>
    <tr>
      <th class="mortgagecalc__table__price"><%= I18n.t("stamp_duty.table.property_price_header") %></th>
      <th class="mortgagecalc__table__rate"><%= I18n.t("stamp_duty.table.rate_header") %></th>
      <th class="mortgagecalc__table__extra"><%= I18n.t("stamp_duty.table.extra_rate_header") %></th>
    </tr>
  </thead>
  <tbody>number_to_currency
    <% @rates.each do |rate| %>
      <tr>
        <td class="mortgagecalc__table__price"><%= number_to_currency(rate[:lower], precision: 0) %> - <%= number_to_currency(rate[:upper], precision: 0) %></td>
        <td class="mortgagecalc__table__rate"><%= rate[:first_home] %>%</td>
        <td class="mortgagecalc__table__extra"><%= rate[:additional_or_buy_to_let] %>%</td>
      </tr>
    <% end %>
  </tbody>
</table>
